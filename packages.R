# packages
if (!require("pacman")) install.packages("pacman")
pacman::p_load(
  tibble,
  ggplot2,
  DT,
  magrittr,
  crayon,
  dplyr,
  tidyr,
  readxl,
  lubridate,
  kableExtra,
  hms
)
